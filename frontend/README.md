# Ampersand + React TodoMVC

To get started:
  1. Make sure you have Gulp globally installed
    - npm install -g gulp
  2. Install local packages
    - npm install
  3. Bundle with Gulp.
    - gulp
